#include <stdio.h>
#include "vector.h"
#include "lem_in.h"
#include "libft.h"

int create_link(t_vector *nodes, char *args)
{
	char **split = ft_split(args, '-');

	if (ft_sstrlen(split) != 2)
	{
		
	}

	return (0);
}